<div>
  <% @students.each do |student| %>
    <div><%= link_to student.to_s, student_path(student) %></div>
  <!--   <div><%= link_to student.to_s, {:action => 'show', :id => student.id} %></div> -->

  <% end %>


</div>
